Contains a small StateSmith project so that we can generate a .html simulator for this FSM.

Generate with StateSmith CLI:
```bash
common/V0-sim$ ss.cli run --here
```

Targets .js just so we don't confuse anyone with .c/.cpp. Someday StateSmith will allow generating just a .sim.html file.
