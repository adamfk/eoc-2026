// Autogenerated with StateSmith 0.22.1-alpha-1+63e5c1c8f4246c6c2d862264aeda8f4ba0a27f54.
// Algorithm: Balanced2. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

#include "V2_ControlSm.h"
#include <stdbool.h> // required for `consume_event` flag
#include <string.h> // for memset

// This function is used when StateSmith doesn't know what the active leaf state is at
// compile time due to sub states or when multiple states need to be exited.
static void exit_up_to_state_handler(V2_ControlSm* sm, V2_ControlSm_StateId desired_state);

static void ROOT_enter(V2_ControlSm* sm);

static void OFF_enter(V2_ControlSm* sm);

static void OFF_exit(V2_ControlSm* sm);

static void OFF_inc(V2_ControlSm* sm);

static void ON_GROUP_enter(V2_ControlSm* sm);

static void ON_GROUP_exit(V2_ControlSm* sm);

static void ON_GROUP_off(V2_ControlSm* sm);

static void ON1_enter(V2_ControlSm* sm);

static void ON1_exit(V2_ControlSm* sm);

static void ON1_dim(V2_ControlSm* sm);

static void ON1_inc(V2_ControlSm* sm);

static void ON2_enter(V2_ControlSm* sm);

static void ON2_exit(V2_ControlSm* sm);

static void ON2_dim(V2_ControlSm* sm);

static void ON2_inc(V2_ControlSm* sm);

static void ON3_enter(V2_ControlSm* sm);

static void ON3_exit(V2_ControlSm* sm);

static void ON3_dim(V2_ControlSm* sm);


// State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
void V2_ControlSm_ctor(V2_ControlSm* sm)
{
    memset(sm, 0, sizeof(*sm));
}

// Starts the state machine. Must be called before dispatching events. Not thread safe.
void V2_ControlSm_start(V2_ControlSm* sm)
{
    ROOT_enter(sm);
    // ROOT behavior
    // uml: TransitionTo(ROOT.<InitialState>)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ROOT.<InitialState>`.
        // ROOT.<InitialState> is a pseudo state and cannot have an `enter` trigger.
        
        // ROOT.<InitialState> behavior
        // uml: TransitionTo(OFF)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `OFF`.
            OFF_enter(sm);
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return;
        } // end of behavior for ROOT.<InitialState>
    } // end of behavior for ROOT
}

// Dispatches an event to the state machine. Not thread safe.
// Note! This function assumes that the `event_id` parameter is valid.
void V2_ControlSm_dispatch_event(V2_ControlSm* sm, V2_ControlSm_EventId event_id)
{
    switch (sm->state_id)
    {
        // STATE: V2_ControlSm
        case V2_ControlSm_StateId_ROOT:
            // No events handled by this state (or its ancestors).
            break;
        
        // STATE: OFF
        case V2_ControlSm_StateId_OFF:
            switch (event_id)
            {
                case V2_ControlSm_EventId_INC: OFF_inc(sm); break;
                
                default: break; // to avoid "unused enumeration value in switch" warning
            }
            break;
        
        // STATE: ON_GROUP
        case V2_ControlSm_StateId_ON_GROUP:
            switch (event_id)
            {
                case V2_ControlSm_EventId_OFF: ON_GROUP_off(sm); break;
                
                default: break; // to avoid "unused enumeration value in switch" warning
            }
            break;
        
        // STATE: ON1
        case V2_ControlSm_StateId_ON1:
            switch (event_id)
            {
                case V2_ControlSm_EventId_DIM: ON1_dim(sm); break;
                case V2_ControlSm_EventId_INC: ON1_inc(sm); break;
                case V2_ControlSm_EventId_OFF: ON_GROUP_off(sm); break; // First ancestor handler for this event
                
                default: break; // to avoid "unused enumeration value in switch" warning
            }
            break;
        
        // STATE: ON2
        case V2_ControlSm_StateId_ON2:
            switch (event_id)
            {
                case V2_ControlSm_EventId_INC: ON2_inc(sm); break;
                case V2_ControlSm_EventId_DIM: ON2_dim(sm); break;
                case V2_ControlSm_EventId_OFF: ON_GROUP_off(sm); break; // First ancestor handler for this event
                
                default: break; // to avoid "unused enumeration value in switch" warning
            }
            break;
        
        // STATE: ON3
        case V2_ControlSm_StateId_ON3:
            switch (event_id)
            {
                case V2_ControlSm_EventId_DIM: ON3_dim(sm); break;
                case V2_ControlSm_EventId_OFF: ON_GROUP_off(sm); break; // First ancestor handler for this event
                
                default: break; // to avoid "unused enumeration value in switch" warning
            }
            break;
    }
    
}

// This function is used when StateSmith doesn't know what the active leaf state is at
// compile time due to sub states or when multiple states need to be exited.
static void exit_up_to_state_handler(V2_ControlSm* sm, V2_ControlSm_StateId desired_state)
{
    while (sm->state_id != desired_state)
    {
        switch (sm->state_id)
        {
            case V2_ControlSm_StateId_OFF: OFF_exit(sm); break;
            
            case V2_ControlSm_StateId_ON_GROUP: ON_GROUP_exit(sm); break;
            
            case V2_ControlSm_StateId_ON1: ON1_exit(sm); break;
            
            case V2_ControlSm_StateId_ON2: ON2_exit(sm); break;
            
            case V2_ControlSm_StateId_ON3: ON3_exit(sm); break;
            
            default: return;  // Just to be safe. Prevents infinite loop if state ID memory is somehow corrupted.
        }
    }
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ROOT
////////////////////////////////////////////////////////////////////////////////

static void ROOT_enter(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ROOT;
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state OFF
////////////////////////////////////////////////////////////////////////////////

static void OFF_enter(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_OFF;
    
    // OFF behavior
    // uml: enter / { Lights_off(); }
    {
        // Step 1: execute action `Lights_off();`
        Lights_off();
    } // end of behavior for OFF
}

static void OFF_exit(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ROOT;
}

static void OFF_inc(V2_ControlSm* sm)
{
    // OFF behavior
    // uml: INC TransitionTo(ON1)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        OFF_exit(sm);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ON1`.
        ON_GROUP_enter(sm);
        ON1_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for OFF
    
    // No ancestor handles this event.
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ON_GROUP
////////////////////////////////////////////////////////////////////////////////

static void ON_GROUP_enter(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON_GROUP;
}

static void ON_GROUP_exit(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ROOT;
}

static void ON_GROUP_off(V2_ControlSm* sm)
{
    // ON_GROUP behavior
    // uml: OFF TransitionTo(OFF)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        exit_up_to_state_handler(sm, V2_ControlSm_StateId_ROOT);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `OFF`.
        OFF_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for ON_GROUP
    
    // No ancestor handles this event.
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ON1
////////////////////////////////////////////////////////////////////////////////

static void ON1_enter(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON1;
    
    // ON1 behavior
    // uml: enter / { Lights_set(1,0,0); }
    {
        // Step 1: execute action `Lights_set(1,0,0);`
        Lights_set(1,0,0);
    } // end of behavior for ON1
}

static void ON1_exit(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON_GROUP;
}

static void ON1_dim(V2_ControlSm* sm)
{
    // ON1 behavior
    // uml: DIM TransitionTo(OFF)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        exit_up_to_state_handler(sm, V2_ControlSm_StateId_ROOT);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `OFF`.
        OFF_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for ON1
    
    // No ancestor handles this event.
}

static void ON1_inc(V2_ControlSm* sm)
{
    // ON1 behavior
    // uml: INC TransitionTo(ON2)
    {
        // Step 1: Exit states until we reach `ON_GROUP` state (Least Common Ancestor for transition).
        ON1_exit(sm);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ON2`.
        ON2_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for ON1
    
    // No ancestor handles this event.
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ON2
////////////////////////////////////////////////////////////////////////////////

static void ON2_enter(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON2;
    
    // ON2 behavior
    // uml: enter / { Lights_set(1,1,0); }
    {
        // Step 1: execute action `Lights_set(1,1,0);`
        Lights_set(1,1,0);
    } // end of behavior for ON2
    
    // ON2 behavior
    // uml: enter / { count = 0; }
    {
        // Step 1: execute action `count = 0;`
        count = 0;
    } // end of behavior for ON2
}

static void ON2_exit(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON_GROUP;
}

static void ON2_dim(V2_ControlSm* sm)
{
    // ON2 behavior
    // uml: DIM TransitionTo(ON1)
    {
        // Step 1: Exit states until we reach `ON_GROUP` state (Least Common Ancestor for transition).
        ON2_exit(sm);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ON1`.
        ON1_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for ON2
    
    // No ancestor handles this event.
}

static void ON2_inc(V2_ControlSm* sm)
{
    // ON2 behavior
    // uml: 1. INC / { count++; }
    {
        // Step 1: execute action `count++;`
        count++;
    } // end of behavior for ON2
    
    // ON2 behavior
    // uml: INC [count >= 3] TransitionTo(ON3)
    if (count >= 3)
    {
        // Step 1: Exit states until we reach `ON_GROUP` state (Least Common Ancestor for transition).
        ON2_exit(sm);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ON3`.
        ON3_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for ON2
    
    // No ancestor handles this event.
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ON3
////////////////////////////////////////////////////////////////////////////////

static void ON3_enter(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON3;
    
    // ON3 behavior
    // uml: enter / { Lights_set(1,1,1); }
    {
        // Step 1: execute action `Lights_set(1,1,1);`
        Lights_set(1,1,1);
    } // end of behavior for ON3
}

static void ON3_exit(V2_ControlSm* sm)
{
    sm->state_id = V2_ControlSm_StateId_ON_GROUP;
}

static void ON3_dim(V2_ControlSm* sm)
{
    // ON3 behavior
    // uml: DIM TransitionTo(ON2)
    {
        // Step 1: Exit states until we reach `ON_GROUP` state (Least Common Ancestor for transition).
        ON3_exit(sm);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ON2`.
        ON2_enter(sm);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        return;
    } // end of behavior for ON3
    
    // No ancestor handles this event.
}

// Thread safe. This function can be disabled with `outputStateIdToStringFunction` setting.
char const * V2_ControlSm_state_id_to_string(V2_ControlSm_StateId id)
{
    switch (id)
    {
        case V2_ControlSm_StateId_ROOT: return "ROOT";
        case V2_ControlSm_StateId_OFF: return "OFF";
        case V2_ControlSm_StateId_ON_GROUP: return "ON_GROUP";
        case V2_ControlSm_StateId_ON1: return "ON1";
        case V2_ControlSm_StateId_ON2: return "ON2";
        case V2_ControlSm_StateId_ON3: return "ON3";
        default: return "?";
    }
}

// Thread safe. This function can be disabled with `outputEventIdToStringFunction` setting.
char const * V2_ControlSm_event_id_to_string(V2_ControlSm_EventId id)
{
    switch (id)
    {
        case V2_ControlSm_EventId_DIM: return "DIM";
        case V2_ControlSm_EventId_INC: return "INC";
        case V2_ControlSm_EventId_OFF: return "OFF";
        default: return "?";
    }
}

// Returns the parent state for a given state. Returns ROOT if input has no parent.
// Thread safe. This function can be disabled with `outputGetParentIdFunction` setting.
V2_ControlSm_StateId V2_ControlSm_get_parent_id(V2_ControlSm_StateId id)
{
    switch (id)
    {
        case V2_ControlSm_StateId_ROOT: return V2_ControlSm_StateId_ROOT;
        case V2_ControlSm_StateId_OFF: return V2_ControlSm_StateId_ROOT;
        case V2_ControlSm_StateId_ON_GROUP: return V2_ControlSm_StateId_ROOT;
        case V2_ControlSm_StateId_ON1: return V2_ControlSm_StateId_ON_GROUP;
        case V2_ControlSm_StateId_ON2: return V2_ControlSm_StateId_ON_GROUP;
        case V2_ControlSm_StateId_ON3: return V2_ControlSm_StateId_ON_GROUP;
        default: return V2_ControlSm_StateId_ROOT;
    }
}
